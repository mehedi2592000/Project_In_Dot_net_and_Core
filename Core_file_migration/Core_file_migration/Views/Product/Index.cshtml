@model IEnumerable<Core_file_migration.Models.Product>

    <p>
        <a asp-action="Create">Create New</a>
    </p>
    <table class="table">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Id)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Name)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.quantity)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.ProductName)
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model) {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Id)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.quantity)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.ProductName)
                </td>
                <td>
                    <img src="~/wwwroot/Content/@item.ProductName"  id="embpdf" class="img" alt="A red rose" width="100px;" height="100px;" />
                </td>
                <td>
                   <a class="btn btn-primary" href="~/Content/@item.ProductName">Read now</a>

                </td>

                


            </tr>
}
        </tbody>
    </table>
    


    <a href="Generatpdf">PDF</a>

     <form methode="post" asp-controller="Product" asp-action="Genpdf">
        <lable>Click hare</lable>
        <input type="submit" value="Generat pdf" />
    </form>

    
